## SOCKS ###################################################################
filter {
  if [event][dataset] == "socks" {

    mutate {
      merge => { "[related][ip]" => "[socks][bound][ip]" }
      merge => { "[related][ip]" => "[socks][request][ip]" }
    }

  }
}