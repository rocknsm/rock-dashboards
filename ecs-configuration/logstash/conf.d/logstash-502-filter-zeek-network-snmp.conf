## SNMP ##################################################################
filter {
  if [event][dataset] == "snmp" {
    if [snmp] {

      mutate {
        rename => { "[snmp][duration]" => "[event][duration]" }
      }

    }
  }
}