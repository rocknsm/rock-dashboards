## SNMP ##################################################################
filter {
  if [event][dataset] == "snmp" {
    if [snmp] {

      mutate {
        copy => {
          "[snmp][duration]" => "[event][duration]"
          "[snmp][display_string]" => "[server][software][original]"
        }
      }

    }
  }
}