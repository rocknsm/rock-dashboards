## Syslog ###################################################################
filter {
  if [event][dataset] == "syslog" {
    if [syslog] {

      mutate {
        rename => { "[syslog][severity]" => "[event][severity]" }
      }

    }
  }
}