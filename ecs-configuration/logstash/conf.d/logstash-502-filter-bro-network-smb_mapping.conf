## SMB_Mapping #############################################################
filter {
  if [event][dataset] == "smb_mapping" {
    if [smb_mapping] {

      mutate {
        copy => {
          "[smb][path]" => "[file][path]"
          "[smb][native_file_system]" => "[file][system]"
        }
      }

    }
  }
}
