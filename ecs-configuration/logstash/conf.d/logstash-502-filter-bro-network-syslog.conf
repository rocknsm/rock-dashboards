## Syslog ###################################################################
filter {
  if [event][dataset] == "syslog" {
    if [syslog] {

      mutate {
        rename => {
          "[syslog][facility]" => "[event][facility]"
          "[syslog][severity]" => "[event][severity]"
        }
      }

    }
  }
}