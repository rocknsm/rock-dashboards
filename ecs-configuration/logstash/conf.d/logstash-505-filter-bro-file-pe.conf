## PE ###################################################################
filter {
  if [event][dataset] == "pe" {

      mutate {
        add_field => { "[file][type]" => "pe" }
        copy => {
          "[log][id][id]" => "[event][id]"
          "[pe][compile_ts]" => "[file][crtime]"
        }
        merge => { "[related][id]" => "[log][id][id]" }
      }

  }
}