## Traceroute ###################################################################
filter {
  if [event][dataset] == "traceroute" {

      mutate {

        rename => { "[traceroute][proto]" => "[network][transport]" }

        copy => {
          "[client][ip]" => "[client][address]"
          "[server][ip]" => "[server][address]"
        }

      }

  }
}