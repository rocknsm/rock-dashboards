filter {
  if [@metadata][stage] == 'bro_tagged_json' {

    # Set the timestamp
    date { match => [ "ts", "ISO8601" ]}

    mutate {
      rename => {
        "@stream" => "[event][category]"
        "@system" => "[event][system]"
        "@proc" => "[event][bro][process]"
      }
    }
  }
}
