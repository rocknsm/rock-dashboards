output {
    if [@metadata][stage] in ["bro_network", "bro_file", "bro_netcontrol", "bro_detection", "bro_obervations", "bro_miscellaneous", "bro_diagnostic"] {
        kafka {
          codec => json
          topic_id => "bro-ecs-%{[event][module]}"
          bootstrap_servers => "127.0.0.1:9092"
        }
    }
}
